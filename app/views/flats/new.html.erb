<!-- new.html.erb -->
<h1 class="mt-5" >Add new flat</h1>
<%= simple_form_for(@flat) do |r| %>
  <%= r.input :name %>
  <%= r.input :address %>
  <%= r.input :description %>
  <%= r.input :price_per_night %>
  <%= r.input :number_of_guests %>
  <%= r.submit %>
<% end %>

